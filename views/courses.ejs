<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses | Demo EJS Education</title>
    <link href="/styles/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/home.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="/styles/all.min.css">
    <!--Page styling inspired by https://getbootstrap.com/docs/4.0/examples/cover/  -->
</head>

<body class="d-flex text-center text-white bg-dark">
    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
        <header class="mb-auto">
            <div>
                <h3 class="float-md-start mb-0">Demo EJS Education</h3>
                <nav class="nav nav-masthead justify-content-center float-md-end">
                    <a class="nav-link" href="/"><i class="fas fa-home"></i> Home</a>
                    <a class="nav-link active" aria-current="page" href="/courses">Courses</a>
                    <a class="nav-link" href="/aboutus">About Us</a>
                    <a class="nav-link" href="/contact">Contact Us</a>
                    <span class="nav-link"> | </span>
                    <% if(!currentUser) { %>
                        <a class="nav-link" href="/login">Login</a>
                        <a class="nav-link" href="/register">Register</a>
                        <% } else if(currentUser.isAdmin==true) { %>
                            <a class="nav-link" href="/tportal">Teacher Portal</a>
                            <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                            <% } else if(currentUser.isAdmin==false) { %>
                                <a class="nav-link" href="/sportal">Student Portal</a>
                                <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                            <% } else { %>
                                <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                                <% } %>
                </nav>
            </div>
        </header>
        <main class="px-3">
            <div class="row">
                <h1 class="mb-4"><strong>Courses</strong></h1>
                <% if(settings !== null) { %> 
                <p class="lead text-start"><strong><i class="<%=settings.courseIcon1%>"></i> <%=settings.courseLabel1.toUpperCase()%> </strong><br><%=settings.courseDesc1%><br><br>

                    <strong><i class="<%=settings.courseIcon2%>"></i> <%=settings.courseLabel2.toUpperCase()%></strong><br><%=settings.courseDesc2%><br><br>

                    <strong><i class="<%=settings.courseIcon3%>"></i> <%=settings.courseLabel3.toUpperCase()%></strong><br><%=settings.courseDesc3%><br><br>

                    <strong><i class="<%=settings.courseIcon4%>"></i> <%=settings.courseLabel4.toUpperCase()%></strong><br><%=settings.courseDesc4%><br><br>

                    <strong><i class="<%=settings.courseIcon5%>"></i> <%=settings.courseLabel5.toUpperCase()%></strong><br><%=settings.courseDesc5%><br><br>

                    <strong><i class="<%=settings.courseIcon6%>"></i> <%=settings.courseLabel6.toUpperCase()%></strong><br><%=settings.courseDesc6%></p>
                    <% } else { %>
                        <p>Please configure the Site Settings in the Teacher Portal.</p>
                        <% } %>  
            </div>
        </main>
        <footer class="mt-auto text-white-50">
            <%- include('./partials/flash.ejs') %>
                <p>&copy; 2021 LibertyLaunchGroup</p>
        </footer>
    </div>
    <script src="/scripts/popper.min.js"></script>
    <script src="/scripts/bootstrap.min.js"></script>
</body>

</html>